spring:
  datasource:
    url: jdbc:h2:mem:timerbook_test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: teste
    password: 123

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false

  flyway:
    enabled: false

app:
  reminders:
    reading:
      enabled: false